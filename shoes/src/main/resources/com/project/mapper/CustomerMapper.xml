<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.mapper.CustomerMapper">

	
	<insert id="reviewInsert">
	<selectKey keyProperty="review_id" order="BEFORE" resultType="int">
			select reviews_seq.nextval from dual
		</selectKey>
		insert into reviews (review_id,regdate,contents, product_id,member_id,jumsu)
		 VALUES (#{reviewId},#{regdate},#{contents},#{productId},#{memberId},{jumsu})
	</insert>
	
</mapper>